// Stub — full implementation lives in Phase 5 once the org plugin is wired.

export default function AcceptInviteStubPage() {
  return (
    <div className="space-y-6 text-center">
      <h1 className="text-2xl font-semibold">Invitation</h1>
      <p className="text-sm text-[var(--color-muted-foreground)]">
        Org-invite acceptance is wired in Phase 5.
      </p>
    </div>
  )
}
